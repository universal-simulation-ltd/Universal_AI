import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ltd.universalsimulation.ai',
  appName: 'Universal AI',
  webDir: 'dist'
};

export default config;
